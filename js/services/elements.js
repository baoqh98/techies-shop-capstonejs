// in product list element
export const productListEl = document.getElementById('productList');

// in product filter element
export const productFilterSelector = document.getElementById('productFilter');

// in render cart
const modalDismissEls = document.querySelectorAll('[data-bs-dismiss]');

export const elementForCloseModal = Array.from(modalDismissEls);
export const overlayModal = document.getElementById('modelId');
export const cartEl = document.getElementById('cart');
export const cartListEl = document.getElementById('cartList');
export const cartCheckoutBtn = document.getElementById('checkoutBtn');
export const cartTotalAmount = document.getElementById('totalAmount');
